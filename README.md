# This repo are just to store my bins

- Link (fish): `sudo ln -sf $(pwd)/* /usr/local/bin/`
  - If you want to link just one file, replact `*` to filename, e.g.
    `sudo ln -sf $(pwd)/4devs /usr/local/bin/`
